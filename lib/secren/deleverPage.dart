import 'package:flutter/material.dart';

class dlevery extends StatefulWidget {
  const dlevery({Key? key}) : super(key: key);

  @override
  State<dlevery> createState() => _dleveryState();
}

class _dleveryState extends State<dlevery> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children:[ Center(
          child: Text("fsdfdsf"),
        ),]
      ),
    );
  }
}
